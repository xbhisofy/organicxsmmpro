import { Navigate } from 'react-router-dom';

// Instagram accounts ab Auto Boost page ke top me manage hote hain.
export default function InstagramPage() {
  return <Navigate to="/auto-boost" replace />;
}
